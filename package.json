{
  "name": "kinro-public-demo",
  "version": "1.0.0",
  "description": "Clean-room TypeScript demo for non-regulated AI sales-agent workflows in insurance distribution.",
  "type": "module",
  "bin": {
    "kinro-public-demo": "dist/src/cli.js"
  },
  "scripts": {
    "build": "rm -rf dist && tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "start": "node dist/src/cli.js",
    "demo": "npm run build && node dist/src/cli.js demo",
    "test": "npm run build && node --test dist/test/*.test.js",
    "validate": "npm run typecheck && npm test && node scripts/validate-clean-room.mjs && npm run demo"
  },
  "keywords": [
    "clean-room",
    "insurance-sales-agent-demo",
    "typescript",
    "synthetic-data"
  ],
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^22.15.30",
    "typescript": "^5.8.3"
  }
}
